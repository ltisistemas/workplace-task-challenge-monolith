services:
  postgres:
    image: postgres:16-alpine
    container_name: workplace-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-workplace_tasks}
      POSTGRES_USER: ${POSTGRES_USER:-workplace}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-workplace}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 3s
      retries: 20

volumes:
  postgres_data:

